import React from 'react';

// Services component is now integrated into Hero, so this can be simplified or removed
const Services = () => {
  return null; // Services are now part of the Hero component
};

export default Services;